syntax="proto2";
package qqnews;

message SimHisFilterReq{
    required bytes devid = 1;
    repeated bytes cmsid = 2;
}

message BatchResult{
    required bytes cmsid = 1;
    required bool exist = 2;
}

message SimHisFilterRsp{
    required uint32 result = 1;
    repeated bytes bloomfilter = 2;
    repeated BatchResult batch_result = 3;
}
