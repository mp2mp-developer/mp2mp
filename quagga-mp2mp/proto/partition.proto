syntax="proto2";
package qqnews;

option cc_generic_services = true;

message PartitionRequest {
    required string channelEnName = 1;
    required string devceid = 2;
    optional string openid = 3;
    optional string qq = 4;
    required string type = 5;
};

message PartitionResponse {
    repeated string expid = 1 ;
};

service PartitionService {
	rpc OnPartition(PartitionRequest) returns (PartitionResponse);
};
